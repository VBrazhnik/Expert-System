const OR = '|';

const XOR = '^';

const AND = '+';

const NOT = '!';

const LEFT_PARENTHESIS = '(';

const RIGHT_PARENTHESIS = ')';

module.exports = {
  OR,
  XOR,
  AND,
  NOT,
  LEFT_PARENTHESIS,
  RIGHT_PARENTHESIS,
};
